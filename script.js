function handleYes() { const responseDiv = document.getElementById('response'); responseDiv.innerHTML = '🎉 You just made me the happiest person! 💕'; createConfetti(); } function handleNo() { const noBtn = document.querySelector('.no-btn'); noBtn.style.position = 'absolute'; noBtn.style.left = Math.random() * 200 + 'px'; noBtn.style.top = Math.random() * 200 + 'px'; } function createConfetti() { for (let i = 0; i < 50; i++) { const confetti = document.createElement('div'); confetti.style.position = 'fixed'; confetti.style.left = Math.random() * 100 + '%'; confetti.style.top = '-10px'; confetti.style.width = '10px'; confetti.style.height = '10px'; confetti.style.backgroundColor = ['#ff6b9d', '#c06c84', '#ffb6d9', '#ffd1e3'][Math.floor(Math.random() * 4)]; confetti.style.borderRadius = '50%'; confetti.style.zIndex = '1000'; document.body.appendChild(confetti); let top = 0; const animation = setInterval(() => { top += Math.random() * 5 + 2; confetti.style.top = top + 'px'; if (top > window.innerHeight) { clearInterval(animation); confetti.remove(); } }, 30); } }